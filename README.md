
# Complex Triggering Application (CTA)

This module provides an epics application and a gui which
can be used to define a sequence of events. This sequence
can be downloaded on an IOC and played out on a mrf
EVG HW.

# Tagging policy
This repository contains several components (e.g. EPICS db app, python cta_lib, python ui).
Each of these components evolve separately and are deployed separately but all components need to fit together to get a working CTA system.

To manage this we use a tagging scheme with the following tags:

  * CTA bundle tags: format = cta_bundle_major.minor.bugfix
  * CTA EPICS DB tags: format = major.minor.bugfix
  * CTA lib tags: format = cta_lib_major.minor.bufgix

The CTA bundle tags mark a state where all the components in the repro fit together.

The CTA EPICS DB tags mark a state of a releasable CTA EPICS database. The major.minor.bugfix tag name is used as EPICS version by the driver.makefile.

The CTA lib tags mark a state of a releasable CTA lib. The major.minor.bugfix part of the CTA lib tag is used as conda package version in lib/conda-recipe/meta.yaml.
=======
The CTA lib tags mark a state of a releasable CTA lib. The major.minor.bugfix part of the CTA lib tag is used as conda package version in lib/conda-recipe/meta.yaml.

## ⚠️ EPICS 7 migration – important architectural note

### Background

The CTA timing logic was historically implemented as a **Quantum Leaps (QP/C) state machine**.
The files `seqCtrl.c` / `seqCtrl.h` are **generated by QM (Quantum Modeler)** and depend on the **QP/C runtime**.

This design originated from an embedded-systems approach and predates EPICS-native state machine solutions.

---

## Why EPICS 3 appeared to work

Under EPICS 3:

- IOC support was **statically linked**
- Execution order was **undefined but permissive**
- Background progress could occur accidentally
- Missing or incorrect scheduling assumptions were masked

As a result, QP/C appeared to function inside the IOC, even though it never truly owned the scheduler.

This behavior was **incidental, not correct**.

## EPICS 7 issue

Upgrading EOS to the newest version together with EPICS 7 implicitly changed the IOC runtime behaviour. As a result, 
CTA started behaving in a non-deterministic manner. This issue is suspected to be linked to the scheduler's ownership 
probblem described above. CTA worked in EPICS3 by coincidence, and changes in the runtime behaviour exposes this issue. 

EOS was rolled back to a simpler version compatible with EPICS 7. CTA was upgraded as described below.  

## What was done during the migration

To make the build reproducible and loadable:

- Regenerated `seqCtrl.c` / `seqCtrl.h` using **QM 5.0.1**
- Vendored a matching **QP/C 6.7.x** runtime
- Selected the **QV (cooperative) kernel**
- Added missing runtime units explicitly
- Implemented required QP hooks (`QF_onStartup`, etc.)

This resulted in:

- Reproducible builds
- IOC loading successfully
- No unresolved symbols

---

## Technical debt

Instead of rebuilding the QP/C-based application with newer version, the implementation shall be **replaced**.

A new CTA version should be developed using the **EPICS Sequencer (SNL)**,
which provides a native, deterministic state-machine model fully compatible
with EPICS 7 and later.

This upcoming version will:
- preserve existing PV interfaces
- remove all QP/C dependencies
- provide correct and deterministic timing behavior

---

## Final note

The current EPICS 7-compatible CTA version should be treated as **transitional**.
